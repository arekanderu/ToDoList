import React from "react";
import EditIcon from '@material-ui/icons/Edit';

const Delete = (props) => {
  return(
    <div className="edit">
       <EditIcon />
    </div>
  )
}

export default Delete;